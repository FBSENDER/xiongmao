- title("#{@sku.title}_熊猫装饰画网")
- head_desc("熊猫装饰画网，为您推荐2020装饰画大全，各种场景与风格，尽量满足您的美感与使用需求，提供装饰画的购买渠道、报价以及图片赏析等服务。装饰画包含黑白、色彩、线描、人物风景、家居家庭、客厅卧室、电表箱、玄关、墙上、手绘壁画印刷装饰画等，请您随意挑选！")
- keywords("#{@sku.title},#{@sku.title}购买指南,#{@sku.title}折扣,#{@sku.title}优惠")
- path(@path)
- h1(@sku.title)

.wrapper
  .product-list-2
    .product-item
      .img-area
        %img{src: "#{@sku.img_url}"}
        .mask
          %p.title #{@sku.title}
          %p.price-area
            %span.price ￥#{@sku.price}
            - if @sku.o_price > 0
              %span.o-price 原价 ￥#{@sku.o_price}
            - else
              %span.o-price 去看看 别错过
    .desc-area
      %p #{@sku.description}
  = render partial: "jd_media/sku_list_1", locals: {skus: @skus}
  = render partial: "jd_media/collection_list", locals: {cs: @cs}
= render partial: "layouts/footer"
